namespace Ecs
{
    /// <summary>
    /// 收集器，Collector收集的是发生了变化的 Entity
    /// 如果一个单位因为中了陷阱或者被束缚、击晕等行为导致被移除了MoveComponent，那么我们就可以使用Collector去收集，然后根据原因作出表现。
    /// </summary>
    public class Collector
    {
        
    }
}